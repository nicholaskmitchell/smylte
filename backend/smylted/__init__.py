"""smylted — the Smylte backend.

Phase 0 is the foundation: a hand-rolled CalDAV client, iCalendar round-trip with
fidelity guarantees, a SQLite cache/sidecar, and a sync engine. No UI, no HTTP
server yet — those arrive with Phase 1+.
"""

__version__ = "0.0.0-phase0"
