# Copy to smylte-cloudflared.env (same dir). The token comes from the Cloudflare
# Zero Trust dashboard when you create the tunnel (Networks -> Tunnels -> Create).
TUNNEL_TOKEN=REPLACE_with_tasks_tunnel_token
